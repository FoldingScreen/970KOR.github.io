(() => {
  const firebaseConfig = { apiKey: "AIzaSyBu2RrQn8cAwwWaLtw5O8Omwn4-NzHWuc0", authDomain: "kor-app-fa47e.firebaseapp.com", projectId: "kor-app-fa47e", storageBucket: "kor-app-fa47e.firebasestorage.app", messagingSenderId: "397749083935", appId: "1:397749083935:web:51c7c" };
  if (!firebase.apps.length) firebase.initializeApp(firebaseConfig);
  firebase.firestore().settings({ experimentalForceLongPolling: true, useFetchStreams: false });
  const db = firebase.firestore(), FV = firebase.firestore.FieldValue, CARD_BASE = "./cards/", MAX_PLAYERS = 8;
  const RANKS = [
    [1,"01","사바나","card-01-sabana.png",1],[2,"02","세자","card-02-prince.png",2],[3,"03","영의정","card-03-yeonguijeong.png",3],[4,"04","관찰사","card-04-governor.png",4],[5,"05","암행어사","card-05-amhaeng.png",5],[6,"06","사또","card-06-satto.png",6],[7,"07","이방","card-07-ibang.png",7],[8,"08","포졸","card-08-pojol.png",8],[9,"09","선비","card-09-seonbi.png",9],[10,"10","상인","card-10-merchant.png",10],[11,"11","농민","card-11-farmer.png",11],[12,"12","노비","card-12-nobi.png",12],[13,"J","홍길동","card-j-hong.png",2]
  ].map(([rank,code,name,image,count]) => ({ rank, code, name, image, count, joker: rank === 13 }));
  const S = { user: "", roomId: localStorage.getItem("dalmutiCurrentRoomId") || "", room: null, participants: [], messages: [], selected: new Map(), unsub: {} };
  const E = {}, $ = id => document.getElementById(id), esc = v => String(v ?? "").replace(/[&<>\"]/g, m => ({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;"}[m]));
  const roomCol = () => db.collection("events").doc("dalmuti").collection("rooms"), roomRef = (id=S.roomId) => roomCol().doc(id), partRef = (id=S.roomId) => roomRef(id).collection("participants"), msgRef = (id=S.roomId) => roomRef(id).collection("messages");
  const rankInfo = r => RANKS.find(x => x.rank === Number(r)) || RANKS[RANKS.length-1], cardImg = r => CARD_BASE + rankInfo(r).image, nowTs = () => firebase.firestore.Timestamp.now();
  const me = () => S.participants.find(p => p.uid === S.user) || null, isHost = () => S.room?.hostUid === S.user;
  const players = () => S.participants.filter(p => p.type === "player" && !p.removedFromRoom).sort((a,b)=>(a.seatOrder??999)-(b.seatOrder??999));
  const spectators = () => S.participants.filter(p => p.type === "spectator" && !p.removedFromRoom).sort((a,b)=>String(a.nickname||"").localeCompare(String(b.nickname||""),"ko"));
  const activePlayers = () => players().filter(p => !p.finished && !p.forfeited);
  function css(){ if($("dalmutiUnifiedCss"))return; const st=document.createElement("style"); st.id="dalmutiUnifiedCss"; st.textContent=`@media(min-width:881px){.dalmuti-app{width:min(1380px,100%);padding:10px 14px}.dalmuti-topbar{margin-bottom:8px}.dalmuti-topbar h1{font-size:23px;margin:0}.room-shell{grid-template-columns:minmax(0,1fr) 300px;gap:10px}.panel{padding:12px;border-radius:18px}.game-panel{min-height:calc(100vh - 92px);display:flex;flex-direction:column}.room-head{display:none!important}.message-bar{margin:0 0 8px;padding:8px 10px;font-size:13px}.table-wrap{height:calc(100vh - 300px);min-height:540px;max-height:620px;margin-top:6px;flex:1}.hand-header{margin-top:8px}.hand-header h3{font-size:17px;margin:0}.hand-header .muted{display:none}.hand-area{min-height:142px;padding:10px;margin:6px 0;gap:8px}.hand-stack{width:74px}.hand-stack img{width:74px;border-radius:8px}.player-box{width:116px;min-height:68px;padding:7px;border-radius:14px;position:absolute}.player-role{font-size:11px}.player-name{font-size:13px}.player-meta{font-size:11px}.seat-bottom{bottom:6px!important}.seat-top-0,.seat-top-1,.seat-top-2{top:6px!important}.seat-left-0,.seat-left-1,.seat-left-2{left:8px!important}.seat-right-0,.seat-right-1,.seat-right-2{right:8px!important}.seat-top-0{left:50%!important;transform:translateX(-50%)!important}.seat-top-1{left:30%!important;transform:translateX(-50%)!important}.seat-top-2{left:70%!important;transform:translateX(-50%)!important}.player-box.submitted{border-color:#7ee2a8!important;box-shadow:0 0 0 2px rgba(126,226,168,.55),0 12px 24px rgba(0,0,0,.28)!important}.player-box.passed{opacity:.8!important;border-color:#8792a7!important;background:rgba(35,39,51,.92)!important}.player-box.forfeited{opacity:.45!important;filter:grayscale(.9)}.badge{display:inline-block;margin-top:3px;padding:2px 7px;border-radius:999px;font-size:10px;font-weight:900}.badge.submit{background:rgba(126,226,168,.16);border:1px solid rgba(126,226,168,.75);color:#9ff0bd}.badge.pass{background:rgba(135,146,167,.16);border:1px solid rgba(135,146,167,.75);color:#d2d8e4}.badge.turn{background:rgba(243,210,129,.16);border:1px solid rgba(243,210,129,.75);color:#f3d281}.center-pile{width:720px!important;max-width:72%!important;height:360px!important;min-height:360px!important;padding:0!important;overflow:visible!important;text-align:left!important}.pile-board{position:relative;width:100%;height:100%}.pile-empty{height:100%;display:flex;align-items:center;justify-content:center;color:#aeb5c3;font-weight:900;font-size:18px}.prev-pile{position:absolute;left:14px;top:14px;width:160px;min-height:158px;border:1px solid rgba(255,255,255,.09);border-radius:14px;background:rgba(0,0,0,.24);padding:10px;display:flex;flex-direction:column;align-items:center}.prev-pile-title{font-size:12px;font-weight:900;color:#aeb5c3;margin-bottom:8px}.prev-pile img{width:82px;border-radius:10px}.cur-pile{position:absolute;left:205px;right:18px;top:20px;bottom:18px;display:flex;flex-direction:column;align-items:center;justify-content:center}.cur-pile-title{font-size:14px;font-weight:900;color:#f3d281;margin-bottom:12px}.cur-cards{display:flex;align-items:center;justify-content:center}.cur-cards img{width:132px;object-fit:cover;border-radius:12px;box-shadow:0 12px 28px rgba(0,0,0,.42);margin-left:-38px}.cur-cards img:first-child{margin-left:0}.side-panel{gap:9px;max-height:calc(100vh - 92px);overflow:auto}.chat-list{height:170px}.side-box{margin-top:8px;border:1px solid rgba(243,210,129,.28);background:rgba(13,19,32,.72);border-radius:16px;padding:10px}.side-title{font-weight:900;color:#f3d281;margin-bottom:7px}.chip{font-size:11px;padding:4px 7px;border-radius:999px;border:1px solid rgba(174,181,195,.28);background:rgba(255,255,255,.045)}.result-row{display:grid;grid-template-columns:40px minmax(0,1fr) 48px 62px;gap:6px;padding:6px 7px;border-radius:12px;background:rgba(255,255,255,.045);font-size:12px;margin-top:5px}.result-row.header{background:transparent;color:#aeb5c3;font-weight:900}.winner{border:1px solid rgba(243,210,129,.55);background:rgba(243,210,129,.1)}.kick-btn{position:absolute;right:5px;top:5px;border:0;border-radius:999px;background:rgba(215,101,101,.92);color:#fff;font-size:10px;font-weight:900;padding:3px 6px;cursor:pointer;z-index:3}#tributePanel{position:fixed;right:326px;bottom:12px;z-index:95;display:none;width:260px;padding:10px;border-radius:18px;border:1px solid rgba(243,210,129,.45);background:rgba(13,19,32,.96);box-shadow:0 16px 42px rgba(0,0,0,.42);color:#f4f1e8}.tribute-title{font-weight:900;color:#f3d281}.tribute-line{font-size:12px;color:#aeb5c3;margin:6px 0}.tribute-cards{display:flex;gap:7px;flex-wrap:wrap}.tribute-cards img{width:40px;border-radius:7px}}#helpModal{position:fixed;inset:0;z-index:160;display:none;background:rgba(0,0,0,.58);align-items:center;justify-content:center;padding:24px}.help-card{width:min(760px,96vw);background:#121827;border:1px solid rgba(243,210,129,.35);border-radius:22px;padding:20px}.help-section{border-top:1px solid rgba(255,255,255,.09);padding-top:12px;margin-top:12px;line-height:1.55;color:#d8deea;font-size:14px}.help-section strong{color:#f3d281}`; document.head.appendChild(st); }
  function toast(t){ E.toast.textContent=t; E.toast.classList.add("show"); clearTimeout(toast.t); toast.t=setTimeout(()=>E.toast.classList.remove("show"),1800); }
  function view(v){ E.lobbyView.classList.toggle("show",v==="lobby"); E.roomView.classList.toggle("show",v==="room"); E.leaveRoomBtn.classList.toggle("hidden",v!=="room"); }
  function roleByIndex(i,n){ const m={2:["사바나","노비"],3:["사바나","농민","노비"],4:["사바나","세자","농민","노비"],5:["사바나","세자","사또","농민","노비"],6:["사바나","세자","암행어사","사또","농민","노비"],7:["사바나","세자","관찰사","암행어사","사또","농민","노비"],8:["사바나","세자","영의정","관찰사","암행어사","사또","농민","노비"]}; return (m[n]||[])[i]||`${i+1}등`; }
  function shuffle(a){ a=a.slice(); for(let i=a.length-1;i>0;i--){ const j=Math.floor(Math.random()*(i+1)); [a[i],a[j]]=[a[j],a[i]]; } return a; }
  function maxRank(n){ return n<=3?8:n<=5?10:12; }
  function sortHand(h=[]){ return h.slice().sort((a,b)=>a.rank-b.rank||String(a.id).localeCompare(String(b.id))); }
  function makeDeck(n){ const deck=[]; RANKS.filter(r=>r.rank<=maxRank(n)).forEach(r=>{ for(let i=1;i<=r.count;i++) deck.push({id:`r${r.rank}-${i}-${Math.random().toString(36).slice(2,8)}`,rank:r.rank,name:r.name}); }); for(let i=1;i<=2;i++) deck.push({id:`j-${i}-${Math.random().toString(36).slice(2,8)}`,rank:13,name:"홍길동",joker:true}); return shuffle(deck); }
  function groups(h=[]){ const m=new Map(); sortHand(h).forEach(c=>{ if(!m.has(c.rank))m.set(c.rank,[]); m.get(c.rank).push(c); }); return [...m.entries()].map(([rank,items])=>({rank,items})); }
  function selectedCards(){ const h=me()?.hand||[], ids=new Set(); S.selected.forEach(arr=>arr.forEach(c=>ids.add(c.id))); return h.filter(c=>ids.has(c.id)); }
  function normalize(cards){ if(!cards.length)return{ok:false,reason:"카드를 선택하세요."}; const ns=cards.filter(c=>!c.joker), rs=[...new Set(ns.map(c=>c.rank))]; if(rs.length>1)return{ok:false,reason:"같은 계급만 함께 낼 수 있습니다."}; if(!ns.length)return{ok:true,effectiveRank:13,effectiveName:"홍길동",count:cards.length,cards}; return{ok:true,effectiveRank:rs[0],effectiveName:rankInfo(rs[0]).name,count:cards.length,cards}; }
  function canPlay(cards){ const c=normalize(cards), cur=S.room?.currentSet; if(!c.ok||!cur)return c; if(c.count!==cur.count)return{ok:false,reason:`이번 판은 ${cur.count}장씩 내야 합니다.`}; if(c.effectiveRank>=cur.effectiveRank)return{ok:false,reason:"더 높은 계급만 낼 수 있습니다."}; return c; }
  function nextAfter(uid){ const a=activePlayers(); if(!a.length)return""; const i=Math.max(0,a.findIndex(p=>p.uid===uid)); return a[(i+1)%a.length]?.uid||a[0]?.uid||""; }
  function myTribute(){ return (S.room?.tribute?.pairs||[]).find(p=>p.toUid===S.user&&!p.returned)||null; }
  function bestTribute(h,c){ return sortHand(h).filter(x=>!x.joker&&x.rank!==13).slice(0,c); }
  async function addSystem(text){ if(S.roomId) await msgRef().add({type:"system",text,createdAt:FV.serverTimestamp()}); }
  async function updateCounts(id=S.roomId){ if(!id)return; const s=await partRef(id).get(), l=s.docs.map(d=>d.data()).filter(p=>!p.removedFromRoom); await roomRef(id).set({playerCount:l.filter(p=>p.type==="player").length,spectatorCount:l.filter(p=>p.type==="spectator").length,updatedAt:FV.serverTimestamp()},{merge:true}); }
  function collect(){ ["lobbyView","roomView","myNickname","roomTitleInput","totalRoundsSelect","turnLimitSelect","roomList","rankPreview","roomStateText","roomTitle","turnBadge","messageBar","lobbyControls","readyBtn","watchBtn","joinAsPlayerBtn","startBtn","betweenControls","nextRoundBtn","resetGameBtn","playersArea","centerPile","handArea","selectedSummary","playControls","playBtn","passBtn","scoreList","chatList","chatInput","sendChatBtn","toggleSpectatorChatBtn","homeBtn","leaveRoomBtn","createRoomBtn","refreshRoomsBtn","toast"].forEach(id=>E[id]=$(id)); }
  function renderRanks(){ E.rankPreview.innerHTML=RANKS.map(r=>`<span class="rank-chip">${r.code}. ${esc(r.name)}</span>`).join(""); }
  function watchRooms(){ if(S.unsub.rooms)S.unsub.rooms(); S.unsub.rooms=roomCol().orderBy("updatedAt","desc").limit(30).onSnapshot(s=>{ const ds=s.docs.filter(d=>!d.data().closed&&d.data().status!=="closed"); E.roomList.innerHTML=ds.length?ds.map(d=>{const r=d.data(), st=({waiting:"대기 중",playing:"진행 중",betweenRounds:"라운드 종료",tributeReturn:"상납 반환",finished:"게임 종료"})[r.status]||r.status;return`<div class="room-item"><div><strong>${esc(r.title||"사바나 달무티")}</strong><div class="room-meta">${st} · 플레이어 ${r.playerCount||0}/${MAX_PLAYERS} · 관전자 ${r.spectatorCount||0} · ${r.totalRounds?`${r.totalRounds}판`:"무제한"}</div></div><button class="btn primary" onclick="Dalmuti.joinRoom('${d.id}')">입장</button></div>`}).join(""):`<div class="muted">생성된 방이 없습니다.</div>`; },console.error); }
  function positioned(){ const ps=players(), mi=Math.max(0,ps.findIndex(p=>p.uid===S.user)), rot=mi>=0?ps.slice(mi).concat(ps.slice(0,mi)):ps, mine=rot[0], o=rot.slice(1); let left=[],top=[],right=[]; if(o.length===1)top=o; else if(o.length===2){left=[o[0]];right=[o[1]];} else if(o.length===3){left=[o[0]];top=[o[1]];right=[o[2]];} else if(o.length===4){left=[o[0]];top=[o[1],o[2]];right=[o[3]];} else if(o.length===5){left=[o[0],o[1]];top=[o[2],o[3]];right=[o[4]];} else if(o.length===6){left=[o[0],o[1]];top=[o[2],o[3]];right=[o[4],o[5]];} else {left=[o[0],o[1]];top=[o[2],o[3],o[4]];right=[o[5],o[6]];} return [...(mine?[{p:mine,cls:"seat-bottom"}]:[]),...left.map((p,i)=>({p,cls:`seat-left-${left.length===1?0:i+1}`})),...top.map((p,i)=>({p,cls:`seat-top-${top.length===1?0:i}`})),...right.map((p,i)=>({p,cls:`seat-right-${right.length===1?0:i+1}`}))]; }
  function renderPlayers(){ E.playersArea.innerHTML=positioned().map(({p,cls})=>{const sub=S.room?.currentSet?.uid===p.uid, state=p.finished?`${p.finishedRank||""}등`:p.passed?"패스":`${p.cardCount||0}장`; let badge=""; if(sub)badge=`<div class="badge submit">제출</div>`; else if(p.passed)badge=`<div class="badge pass">패스</div>`; else if(S.room?.currentTurnUid===p.uid)badge=`<div class="badge turn">차례</div>`; const kick=isHost()&&p.uid!==S.user?`<button class="kick-btn" onclick="Dalmuti.kick('${p.uid}')">강퇴</button>`:""; return`<div class="player-box ${cls} ${p.uid===S.user?"me":""} ${S.room?.currentTurnUid===p.uid?"turn":""} ${p.passed?"passed":""} ${p.finished?"finished":""} ${sub?"submitted":""} ${p.forfeited?"forfeited":""}">${kick}<div class="player-role">${esc(p.role||"참가자")}</div><div class="player-name">${esc(p.nickname)}</div><div class="player-meta">${state}${p.isReady?" · 준비":""}</div>${badge}</div>`}).join(""); }
  function renderPile(){ if(S.room?.status==="tributeReturn"){const ps=S.room.tribute?.pairs||[]; E.centerPile.innerHTML=`<div class="pile-title">상납 반환</div><div class="muted">상납 받은 사람이 돌려줄 카드를 선택합니다.</div><div class="muted">${ps.map(p=>`${esc(p.fromNickname)} → ${esc(p.toNickname)} ${p.count}장 ${p.returned?"완료":"대기"}`).join("<br>")}</div>`; return;} const prev=S.room?.previousSet, cur=S.room?.currentSet; if(!prev&&!cur){E.centerPile.innerHTML=`<div class="pile-board"><div class="pile-empty">새 판</div></div>`;return;} const one=prev?.cards?.[0]?`<img src="${cardImg(prev.cards[0].rank)}">`:`<span class="muted">없음</span>`, curCards=cur?(cur.cards||[]).map(c=>`<img src="${cardImg(c.rank)}">`).join(""):`<span class="muted">제출 대기</span>`; E.centerPile.innerHTML=`<div class="pile-board"><div class="prev-pile"><div class="prev-pile-title">직전 카드</div>${one}</div><div class="cur-pile"><div class="cur-pile-title">${cur?`현재 ${rankInfo(cur.effectiveRank).name} ${cur.count}장`:"현재 없음"}</div><div class="cur-cards">${curCards}</div></div></div>`; }
  function selectable(g){ if(S.room?.status==="tributeReturn")return!!myTribute(); if(S.room?.status!=="playing"||S.room.currentTurnUid!==S.user||!S.room.currentSet)return true; if(g.rank===13)return false; return g.items.length+(me()?.hand||[]).filter(c=>c.joker).length>=S.room.currentSet.count&&g.rank<S.room.currentSet.effectiveRank; }
  function renderHand(){ const m=me(); if(!m||m.type!=="player"||m.removedFromRoom){E.handArea.innerHTML=`<div class="muted">관전자는 손패가 없습니다.</div>`;E.selectedSummary.textContent="선택 없음";return;} const gs=groups(m.hand||[]); E.handArea.classList.toggle("has-selection",S.selected.size>0); E.handArea.innerHTML=gs.length?gs.map(g=>{const sel=S.selected.get(g.rank)?.length||0;return`<div class="hand-stack${sel?" selected":""}${selectable(g)?"":" disabled"}" onclick="Dalmuti.toggleRank(${g.rank})">${sel?`<span class="stack-selected">${sel}</span>`:""}<img src="${cardImg(g.rank)}"><span class="stack-count">x${g.items.length}</span></div>`}).join(""):`<div class="muted">손패가 없습니다.</div>`; const cards=selectedCards(); if(S.room?.status==="tributeReturn"){const p=myTribute();E.selectedSummary.textContent=p?`${cards.length}/${p.count}장 반환 선택`:"상납 반환 대기";return;} const c=canPlay(cards); E.selectedSummary.textContent=cards.length?(c.ok?`${rankInfo(c.effectiveRank).name} ${c.count}장`:c.reason):"선택 없음"; }
  function renderScores(){ E.scoreList.innerHTML=players().length?players().map(p=>`<div class="score-row"><span>${esc(p.role||"-")} · ${esc(p.nickname)}</span><strong>${p.score||0}</strong></div>`).join(""):`<div class="muted">참가자가 없습니다.</div>`; }
  function renderControls(){ const m=me(), waiting=S.room?.status==="waiting", between=S.room?.status==="betweenRounds"||S.room?.status==="finished", myTurn=S.room?.status==="playing"&&S.room.currentTurnUid===S.user&&m?.type==="player"&&!m.finished&&!m.forfeited, tributeTurn=S.room?.status==="tributeReturn"&&!!myTribute(); E.lobbyControls.classList.toggle("hidden",!waiting);E.betweenControls.classList.toggle("hidden",!between);E.playControls.classList.toggle("hidden",!(myTurn||tributeTurn));E.passBtn.classList.toggle("hidden",!myTurn);E.playBtn.textContent=tributeTurn?"반환 카드 주기":"선택 카드 내기";E.readyBtn.classList.toggle("hidden",!(waiting&&m?.type==="player"));E.watchBtn.classList.toggle("hidden",!(waiting&&m?.type==="player"));E.joinAsPlayerBtn.classList.toggle("hidden",!(waiting&&m?.type==="spectator"));E.startBtn.classList.toggle("hidden",!(waiting&&isHost()));E.nextRoundBtn.classList.toggle("hidden",!(S.room?.status==="betweenRounds"&&isHost()));E.resetGameBtn.classList.toggle("hidden",!isHost());E.readyBtn.textContent=m?.isReady?"준비 취소":"준비"; }
  function renderMessages(){ const docs=S.messages.slice().reverse(); E.chatList.innerHTML=docs.length?docs.map(m=>m.type==="system"?`<div class="chat-msg system">${esc(m.text)}</div>`:`<div class="chat-msg"><span class="chat-name">${esc(m.nickname||"-")}</span> ${esc(m.text||"")}</div>`).join(""):`<div class="muted">채팅이 없습니다.</div>`; E.chatList.scrollTop=E.chatList.scrollHeight; }
  function sideBox(id, after){ let b=$(id); if(!b){b=document.createElement("section");b.id=id;b.className="side-box";(after||document.querySelector(".side-panel")).insertAdjacentElement(after?"afterend":"afterbegin",b);} return b; }
  function renderSide(){ const side=document.querySelector(".side-panel"); if(!side||!S.room)return; const turn=S.room.currentTurnUid?(S.participants.find(p=>p.uid===S.room.currentTurnUid)?.nickname||"-"):"-", st=({waiting:"대기 중",playing:`${S.room.round||1}라운드`,tributeReturn:"상납 반환",betweenRounds:"라운드 종료",finished:"게임 종료"})[S.room.status]||S.room.status; sideBox("roomInfo").innerHTML=`<div class="side-title">방 정보</div><div class="score-row"><span>방제</span><strong>${esc(S.room.title||"-")}</strong></div><div class="score-row"><span>상태</span><strong>${esc(st)}</strong></div><div class="score-row"><span>차례</span><strong>${esc(turn)}</strong></div>${(isHost() || S.user === "병풍") ? `<button class="btn danger small" onclick="Dalmuti.deleteRoom()">방 삭제</button>` : ""}`; const set=sideBox("roomSettings",$("roomInfo")); set.style.display=isHost()&&S.room.status==="waiting"?"block":"none"; if(set.style.display!=="none"){set.innerHTML=`<div class="side-title">방 설정</div><input id="setTitle" class="input" maxlength="24" value="${esc(S.room.title||"")}"><select id="setRounds" class="input"><option value="3">3판</option><option value="5">5판</option><option value="10">10판</option><option value="0">무제한</option></select><div style="display:flex;gap:6px;margin-top:8px"><button class="btn primary small" onclick="Dalmuti.saveSettings()">저장</button><button class="btn ghost small" onclick="Dalmuti.toggleSpectatorChat()">관전자 채팅 ${S.room.spectatorChatEnabled===false?"차단":"허용"}</button></div>`;$("setRounds").value=String(S.room.totalRounds||0);} const sp=sideBox("spectatorPanel",E.scoreList.parentElement); const ss=spectators(); sp.innerHTML=`<div class="side-title">관전자</div>${ss.length?ss.map(p=>`<span class="chip">${esc(p.nickname)}</span>`).join(" "):`<div class="muted">관전자가 없습니다.</div>`}`; const rr=sideBox("roundResultPanel",sp); if(["betweenRounds","finished"].includes(S.room.status)&&S.room.lastRoundResult){const rows=players().slice().sort((a,b)=>(a.lastRoundRank??999)-(b.lastRoundRank??999)); rr.style.display="block"; rr.innerHTML=`<div class="side-title">라운드 결과</div><div class="result-row header"><span>순위</span><span>닉네임</span><span>획득</span><span>계급</span></div>${rows.map(p=>`<div class="result-row"><span>${p.lastRoundRank||"-"}등</span><span>${esc(p.nickname)}</span><strong>+${p.lastRoundScore||0}</strong><span>${esc(p.role||"-")}</span></div>`).join("")}`;}else rr.style.display="none"; }
  function renderTribute(){ let p=$("tributePanel"); if(!p){p=document.createElement("div");p.id="tributePanel";document.body.appendChild(p);} if(!S.room||S.room.status!=="tributeReturn"||!S.room.tribute){p.style.display="none";return;} const inc=myTribute(), outs=(S.room.tribute.pairs||[]).filter(x=>x.fromUid===S.user); if(!inc&&!outs.length){p.style.display="none";return;} p.innerHTML=outs.map(x=>`<div><div class="tribute-title">내가 상납한 카드</div><div class="tribute-line">${esc(x.toNickname)}님에게 ${x.count}장 상납</div><div class="tribute-cards">${(x.cards||[]).map(c=>`<img src="${cardImg(c.rank)}">`).join("")}</div></div>`).join("")+(inc?`<div><div class="tribute-title">상납받은 카드</div><div class="tribute-line">${esc(inc.fromNickname)}님에게서 ${inc.count}장 받음 · 돌려줄 카드 ${inc.count}장 선택</div><div class="tribute-cards">${(inc.cards||[]).map(c=>`<img src="${cardImg(c.rank)}">`).join("")}</div></div>`:""); p.style.display="block"; }
  function msgText(){ const m=me(); if(!S.room)return""; if(S.room.status==="waiting")return m?.type==="spectator"?"관전 중입니다. 참가하려면 참가하기를 누르세요.":"준비를 누르거나 관전하기를 선택하세요."; if(S.room.status==="tributeReturn")return myTribute()?"상납으로 받은 만큼 돌려줄 카드를 선택하세요.":"상납 반환을 기다리는 중입니다."; if(S.room.status==="betweenRounds")return"라운드가 끝났습니다. 다음 계급과 승점을 확인하세요."; if(S.room.status==="finished")return"게임이 종료되었습니다."; return S.room.currentTurnUid===S.user?"내 차례입니다.":"다른 플레이어 차례입니다."; }
  function render(){ if(!S.room)return; if(S.room.closed||S.room.status==="closed"){alert("방이 삭제되었습니다.");leaveLocal();return;} if(S.room.kickedUids?.includes(S.user)){alert("방장에 의해 방에서 내보내졌습니다.");leaveLocal();return;} const st=({waiting:"대기 중",playing:`${S.room.round||1}라운드 진행 중`,tributeReturn:"상납 반환",betweenRounds:"라운드 종료",finished:"게임 종료"})[S.room.status]||S.room.status; E.roomStateText.textContent=st; E.roomTitle.textContent=S.room.title||"사바나 달무티"; E.turnBadge.textContent=S.room.status==="playing"?`차례: ${S.participants.find(p=>p.uid===S.room.currentTurnUid)?.nickname||"-"}`:st; E.messageBar.textContent=msgText(); renderPlayers();renderPile();renderScores();renderHand();renderControls();renderSide();renderTribute(); }
  async function createRoom(){ const title=E.roomTitleInput.value.trim()||`${S.user}의 사바나 달무티`, raw=Number(E.totalRoundsSelect.value||5); const doc=await roomCol().add({title,hostUid:S.user,hostNickname:S.user,status:"waiting",closed:false,round:0,totalRounds:raw===0?null:raw,playerLimit:MAX_PLAYERS,playerCount:1,spectatorCount:0,currentTurnUid:null,currentSet:null,previousSet:null,finishOrder:[],lastRoundResult:null,tribute:null,spectatorChatEnabled:true,kickedUids:[],createdAt:FV.serverTimestamp(),updatedAt:FV.serverTimestamp()}); S.roomId=doc.id;localStorage.setItem("dalmutiCurrentRoomId",S.roomId); await partRef().doc(S.user).set(basePart("player",{seatOrder:0})); await addSystem(`${S.user}님이 방을 만들었습니다.`); enterRoom(S.roomId); }
  function basePart(type,extra={}){return{uid:S.user,nickname:S.user,type,isReady:false,seatOrder:type==="player"?999:null,role:null,score:0,lastRoundScore:0,lastRoundRank:null,hand:[],cardCount:0,passed:false,finished:false,finishedRank:null,forfeited:false,removedFromRoom:false,connected:true,joinedAt:FV.serverTimestamp(),updatedAt:FV.serverTimestamp(),...extra};}
  async function joinRoom(id){ const rs=await roomRef(id).get(); if(!rs.exists||rs.data().closed||rs.data().status==="closed")return toast("입장할 수 없는 방입니다."); if(rs.data().kickedUids?.includes(S.user))return toast("이 방에서 내보내진 상태입니다."); if(S.roomId&&S.roomId!==id)await leaveRoom(false); S.roomId=id;localStorage.setItem("dalmutiCurrentRoomId",id); const pr=partRef(id).doc(S.user), ps=await pr.get(), r=rs.data(); if(ps.exists)await pr.set({connected:true,removedFromRoom:false,updatedAt:FV.serverTimestamp()},{merge:true}); else await pr.set(basePart(r.status==="waiting"?"player":"spectator",{seatOrder:r.status==="waiting"?(r.playerCount||0):null})); await msgRef(id).add({type:"system",text:`${S.user}님이 입장했습니다.`,createdAt:FV.serverTimestamp()}); await updateCounts(id); enterRoom(id); }
  function enterRoom(id){ S.roomId=id;view("room");S.selected.clear();Object.values(S.unsub).forEach(fn=>typeof fn==="function"&&fn());S.unsub={};S.unsub.room=roomRef(id).onSnapshot(s=>{if(!s.exists)return leaveLocal();S.room={id:s.id,...s.data()};render();},console.error);S.unsub.participants=partRef(id).onSnapshot(s=>{S.participants=s.docs.map(d=>({id:d.id,...d.data()}));render();},console.error);S.unsub.messages=msgRef(id).orderBy("createdAt","desc").limit(100).onSnapshot(s=>{S.messages=s.docs.map(d=>d.data());renderMessages();},console.error);}
  async function leaveRoom(write=true){ if(!S.roomId)return; const old=S.roomId; if(me()){await partRef(old).doc(S.user).delete().catch(()=>null); if(write)await msgRef(old).add({type:"system",text:`${S.user}님이 방을 나갔습니다.`,createdAt:FV.serverTimestamp()}).catch(()=>null); if(S.room?.hostUid===S.user)await assignHost(old); await updateCounts(old).catch(()=>null);} leaveLocal();}
  function leaveLocal(){Object.values(S.unsub).forEach(fn=>typeof fn==="function"&&fn());S.unsub={};S.roomId="";S.room=null;S.participants=[];S.messages=[];S.selected.clear();localStorage.removeItem("dalmutiCurrentRoomId");view("lobby");watchRooms();}
  async function assignHost(id=S.roomId){ const snap=await partRef(id).get(), l=snap.docs.map(d=>d.data()).filter(p=>!p.removedFromRoom), n=l.sort((a,b)=>(a.type!==b.type?(a.type==="player"?-1:1):(a.seatOrder??999)-(b.seatOrder??999)))[0]; if(n)await roomRef(id).set({hostUid:n.uid,hostNickname:n.nickname,updatedAt:FV.serverTimestamp()},{merge:true}); else await roomRef(id).set({closed:true,status:"closed",updatedAt:FV.serverTimestamp()},{merge:true});}
  async function toggleReady(){const m=me();if(m&&S.room?.status==="waiting"&&m.type==="player")await partRef().doc(S.user).set({isReady:!m.isReady,updatedAt:FV.serverTimestamp()},{merge:true});}
  async function becomeSpectator(){if(S.room?.status!=="waiting")return;await partRef().doc(S.user).set({type:"spectator",isReady:false,seatOrder:null,role:null,hand:[],cardCount:0,updatedAt:FV.serverTimestamp()},{merge:true});await updateCounts();}
  async function becomePlayer(){if(S.room?.status!=="waiting")return;if(players().length>=MAX_PLAYERS)return toast("최대 8명까지 참가할 수 있습니다.");await partRef().doc(S.user).set({type:"player",isReady:false,seatOrder:players().length,updatedAt:FV.serverTimestamp()},{merge:true});await updateCounts();}
  async function startGame(){if(!isHost()||S.room?.status!=="waiting")return;const ps=players();if(ps.length<2)return toast("2명 이상 필요합니다.");if(!ps.every(p=>p.isReady))return toast("아직 준비하지 않은 인원이 있습니다.");await startRound(1,true,false);}
  function hasHong2(h=[]){return h.filter(c=>c.joker||c.rank===13).length>=2;} function forceHong(hands,uid){let js=[];Object.keys(hands).forEach(u=>{const k=[];hands[u].forEach(c=>{if((c.joker||c.rank===13)&&js.length<2)js.push(c);else k.push(c)});hands[u]=k});while(js.length<2)js.push({id:`j-force-${js.length}-${Math.random().toString(36).slice(2,8)}`,rank:13,name:"홍길동",joker:true});const out=(hands[uid]||[]).filter(c=>!(c.joker||c.rank===13)).sort((a,b)=>b.rank-a.rank).slice(0,2),ids=new Set(out.map(c=>c.id));hands[uid]=(hands[uid]||[]).filter(c=>!ids.has(c.id)).concat(js.slice(0,2));const ds=Object.keys(hands).filter(u=>u!==uid);out.forEach((c,i)=>{if(ds[i%ds.length])hands[ds[i%ds.length]].push(c)});Object.keys(hands).forEach(u=>hands[u]=sortHand(hands[u]));}
  function tributePairs(ps,hands){if(ps.length<3)return[];const specs=ps.length===3?[{from:ps[2],to:ps[0],count:1}]:[{from:ps[ps.length-1],to:ps[0],count:2},{from:ps[ps.length-2],to:ps[1],count:1}];return specs.map((x,i)=>{const cards=bestTribute(hands[x.from.uid],x.count),ids=new Set(cards.map(c=>c.id));hands[x.from.uid]=sortHand(hands[x.from.uid].filter(c=>!ids.has(c.id)));hands[x.to.uid]=sortHand(hands[x.to.uid].concat(cards));return{id:`tribute-${i}`,fromUid:x.from.uid,fromNickname:x.from.nickname,toUid:x.to.uid,toNickname:x.to.nickname,count:cards.length,cards,returned:cards.length===0,returnedCards:[]}}).filter(p=>p.count>0);}
  async function startRound(round,reset,force){let ps=players().slice().sort((a,b)=>(a.lastRoundRank??a.seatOrder??999)-(b.lastRoundRank??b.seatOrder??999));const deck=makeDeck(ps.length),hands=Object.fromEntries(ps.map(p=>[p.uid,[]]));deck.forEach((c,i)=>hands[ps[i%ps.length].uid].push(c));Object.keys(hands).forEach(u=>hands[u]=sortHand(hands[u]));if(force&&ps.length>=3)forceHong(hands,ps[ps.length-1].uid);const lows=ps.length===3?[ps[1]?.uid,ps[2]?.uid]:[ps[ps.length-2]?.uid,ps[ps.length-1]?.uid],reb=round>1&&lows.some(u=>u&&hasHong2(hands[u]));if(reb)ps=ps.slice().reverse();const pairs=round>1?tributePairs(ps,hands):[],hasTrib=pairs.some(p=>!p.returned),batch=db.batch();batch.set(roomRef(),{status:hasTrib?"tributeReturn":"playing",round,currentTurnUid:hasTrib?null:ps[0]?.uid||null,currentSet:null,previousSet:null,finishOrder:[],turnOrder:ps.map(p=>p.uid),tribute:hasTrib?{phase:"return",pairs,reversed:reb,returnStartedAt:nowTs()}:null,updatedAt:FV.serverTimestamp()},{merge:true});ps.forEach((p,i)=>{const hand=sortHand(hands[p.uid]);batch.set(partRef().doc(p.uid),{type:"player",seatOrder:i,role:roleByIndex(i,ps.length),score:reset?0:(p.score||0),hand,cardCount:hand.length,isReady:false,passed:false,finished:false,finishedRank:null,forfeited:false,lastRoundRank:reset?null:p.lastRoundRank,updatedAt:FV.serverTimestamp()},{merge:true})});await batch.commit();await addSystem(reb?"홍길동 출현! 민란이 발생해 계급 순서가 뒤집혔습니다.":(hasTrib?`${round}라운드 상납 반환을 시작합니다.`:`${round}라운드가 시작되었습니다.`));}
  async function nextRound(force=false){if(isHost()&&S.room?.status==="betweenRounds")await startRound((S.room.round||0)+1,false,force);}
  function toggleTribute(rank,g){const p=myTribute();if(!p)return toast("반환할 차례가 아닙니다.");const cur=S.selected.get(rank)||[];if(cur.length)S.selected.delete(rank);else{const left=Math.max(0,p.count-selectedCards().length);if(left<=0)return toast(`${p.count}장만 선택할 수 있습니다.`);S.selected.set(rank,g.items.slice(0,left));}renderHand();}
  function toggleRank(rank){const m=me();if(!m||m.type!=="player")return;const g=groups(m.hand||[]).find(x=>x.rank===rank);if(!g)return;if(S.room?.status==="tributeReturn")return toggleTribute(rank,g);if(S.room?.status==="playing"&&S.room.currentTurnUid===S.user&&S.room.currentSet){if(!selectable(g))return toast("낼 수 없는 계급입니다.");S.selected.clear();const need=S.room.currentSet.count,ns=g.items.filter(c=>!c.joker).slice(0,need),js=(m.hand||[]).filter(c=>c.joker).slice(0,Math.max(0,need-ns.length));S.selected.set(rank,ns.concat(js));return renderHand();}if(S.selected.has(rank))S.selected.delete(rank);else if(rank===13)S.selected.set(rank,g.items.slice());else{const js=S.selected.get(13)||[];S.selected.clear();S.selected.set(rank,g.items.slice());if(js.length)S.selected.set(13,js);}renderHand();}
  async function playSelected(){if(S.room?.status==="tributeReturn")return returnTribute();const m=me();if(!m||S.room?.currentTurnUid!==S.user||m.type!=="player")return;const cards=selectedCards(),combo=canPlay(cards);if(!combo.ok)return toast(combo.reason);const ids=new Set(cards.map(c=>c.id)),newHand=(m.hand||[]).filter(c=>!ids.has(c.id)),order=(S.room.finishOrder||[]).slice();let finRank=m.finishedRank||null;const fin=newHand.length===0;if(fin&&!m.finished){finRank=order.length+1;order.push({uid:S.user,nickname:m.nickname,rank:finRank,finishedAt:nowTs()})}const remain=players().filter(p=>p.uid!==S.user&&!p.finished&&!p.forfeited).length+(fin?0:1),set={uid:S.user,nickname:m.nickname,effectiveRank:combo.effectiveRank,effectiveName:combo.effectiveName,count:combo.count,cards,createdAt:nowTs()},batch=db.batch();batch.set(partRef().doc(S.user),{hand:newHand,cardCount:newHand.length,passed:false,finished:fin,finishedRank:finRank,updatedAt:FV.serverTimestamp()},{merge:true});players().forEach(p=>{if(p.uid!==S.user)batch.set(partRef().doc(p.uid),{passed:false},{merge:true})});if(remain<=1){const last=players().find(p=>p.uid!==S.user&&!p.finished&&!p.forfeited),final=order.slice();if(last)final.push({uid:last.uid,nickname:last.nickname,rank:final.length+1,finishedAt:nowTs()});batch.set(roomRef(),{status:"betweenRounds",currentTurnUid:null,previousSet:S.room.currentSet||null,currentSet:set,finishOrder:final,updatedAt:FV.serverTimestamp()},{merge:true});await batch.commit();return finishRound(final)}batch.set(roomRef(),{previousSet:S.room.currentSet||null,currentSet:set,currentTurnUid:nextAfter(S.user),finishOrder:order,updatedAt:FV.serverTimestamp()},{merge:true});S.selected.clear();await batch.commit();}
  async function returnTribute(){const p=myTribute(),m=me();if(!p||!m)return;const cards=selectedCards();if(cards.length!==p.count)return toast(`${p.count}장을 선택해야 합니다.`);const ids=new Set(cards.map(c=>c.id)),myHand=(m.hand||[]).filter(c=>!ids.has(c.id)),from=S.participants.find(x=>x.uid===p.fromUid),fromHand=sortHand([...(from?.hand||[]),...cards]),pairs=(S.room.tribute?.pairs||[]).map(x=>x.id===p.id?{...x,returned:true,returnedCards:cards}:x),done=pairs.every(x=>x.returned),batch=db.batch();batch.set(partRef().doc(S.user),{hand:myHand,cardCount:myHand.length,updatedAt:FV.serverTimestamp()},{merge:true});batch.set(partRef().doc(p.fromUid),{hand:fromHand,cardCount:fromHand.length,updatedAt:FV.serverTimestamp()},{merge:true});batch.set(roomRef(),{tribute:{...(S.room.tribute||{}),pairs},status:done?"playing":"tributeReturn",currentTurnUid:done?players()[0]?.uid||null:null,updatedAt:FV.serverTimestamp()},{merge:true});S.selected.clear();await batch.commit();if(done)await addSystem(`${S.room.round}라운드가 시작되었습니다.`);}
  async function passTurn(){const m=me();if(!m||S.room?.currentTurnUid!==S.user)return;if(!S.room.currentSet)return toast("새 판에서는 패스할 수 없습니다.");const active=activePlayers().map(p=>p.uid),others=active.filter(uid=>uid!==S.room.currentSet.uid),passed=new Set(players().filter(p=>p.passed).map(p=>p.uid));passed.add(S.user);const over=others.every(uid=>passed.has(uid)),batch=db.batch();batch.set(partRef().doc(S.user),{passed:true,updatedAt:FV.serverTimestamp()},{merge:true});if(over){players().forEach(p=>batch.set(partRef().doc(p.uid),{passed:false},{merge:true}));const starter=S.room.currentSet.uid,sp=players().find(p=>p.uid===starter&&!p.finished&&!p.forfeited);batch.set(roomRef(),{currentTurnUid:sp?starter:nextAfter(starter),previousSet:S.room.currentSet,currentSet:null,updatedAt:FV.serverTimestamp()},{merge:true})}else batch.set(roomRef(),{currentTurnUid:nextAfter(S.user),updatedAt:FV.serverTimestamp()},{merge:true});await batch.commit();}
  async function finishRound(order){const total=players().length,batch=db.batch();order.forEach((r,i)=>{const score=total-i;batch.set(partRef().doc(r.uid),{score:FV.increment(score),lastRoundScore:score,lastRoundRank:i+1,role:roleByIndex(i,total),seatOrder:i,finished:true,finishedRank:i+1,updatedAt:FV.serverTimestamp()},{merge:true})});const done=S.room.totalRounds&&S.room.round>=S.room.totalRounds;batch.set(roomRef(),{status:done?"finished":"betweenRounds",currentTurnUid:null,currentSet:null,previousSet:null,tribute:null,finishOrder:order,lastRoundResult:{round:S.room.round,results:order,endedAt:nowTs()},updatedAt:FV.serverTimestamp()},{merge:true});await batch.commit();await addSystem(done?"게임이 종료되었습니다.":`${S.room.round}라운드가 종료되었습니다.`);}
  async function resetGame(){if(!isHost()||!confirm("현재 진행 상황과 누적 승점이 모두 초기화됩니다. 새 게임으로 초기화할까요?"))return;const batch=db.batch();batch.set(roomRef(),{status:"waiting",round:0,currentTurnUid:null,currentSet:null,previousSet:null,tribute:null,finishOrder:[],lastRoundResult:null,updatedAt:FV.serverTimestamp()},{merge:true});S.participants.forEach(p=>batch.set(partRef().doc(p.uid),{isReady:false,role:null,score:0,lastRoundScore:0,lastRoundRank:null,hand:[],cardCount:0,passed:false,finished:false,finishedRank:null,forfeited:false,updatedAt:FV.serverTimestamp()},{merge:true}));await batch.commit();await addSystem("방장이 새 게임으로 초기화했습니다.");}
  async function sendChat(){const t=E.chatInput.value.trim();if(!t||!S.roomId)return;const m=me();if(m?.type==="spectator"&&S.room?.spectatorChatEnabled===false)return toast("관전자 채팅이 차단되어 있습니다.");E.chatInput.value="";await msgRef().add({type:"chat",uid:S.user,nickname:m?.nickname||S.user,text:t,createdAt:FV.serverTimestamp()});}
  async function saveSettings(){if(!isHost()||S.room?.status!=="waiting")return;const raw=Number($("setRounds")?.value||5);await roomRef().set({title:($("setTitle")?.value||"사바나 달무티").trim(),totalRounds:raw===0?null:raw,updatedAt:FV.serverTimestamp()},{merge:true});await addSystem("방 설정이 변경되었습니다.");}
  async function toggleSpectatorChat(){if(isHost())await roomRef().set({spectatorChatEnabled:S.room?.spectatorChatEnabled===false,updatedAt:FV.serverTimestamp()},{merge:true});}
  async function kick(uid){if(!isHost()||uid===S.user)return;const t=S.participants.find(p=>p.uid===uid);if(!t||!confirm(`${t.nickname}님을 방에서 내보낼까요?`))return;await partRef().doc(uid).set({removedFromRoom:true,kicked:true,updatedAt:FV.serverTimestamp()},{merge:true});await roomRef().set({kickedUids:FV.arrayUnion(uid),updatedAt:FV.serverTimestamp()},{merge:true});await addSystem(`${t.nickname}님이 방장에 의해 강퇴되었습니다.`);await updateCounts();}
  async function deleteRoom(){
  if(!(isHost() || S.user === "병풍")) return toast("방장 또는 병풍만 방을 삭제할 수 있습니다.");

  if(!confirm("방을 완전히 삭제할까요? 참가자, 채팅, 진행 상황이 모두 삭제됩니다.")) return;

  const id = S.roomId;
  if(!id) return;

  const ref = roomRef(id);

  async function deleteSubCollection(colRef){
    while(true){
      const snap = await colRef.limit(300).get();
      if(snap.empty) break;

      const batch = db.batch();
      snap.docs.forEach(doc => batch.delete(doc.ref));
      await batch.commit();
    }
  }

  await deleteSubCollection(ref.collection("messages"));
  await deleteSubCollection(ref.collection("participants"));
  await ref.delete();

  localStorage.removeItem("dalmutiCurrentRoomId");
  alert("방이 완전히 삭제되었습니다.");
  leaveLocal();
}
  function ensureHelp(){if(!$("helpBtn")){const b=document.createElement("button");b.id="helpBtn";b.className="btn ghost";b.type="button";b.textContent="게임 방법";document.querySelector(".top-actions")?.insertBefore(b,document.querySelector(".top-actions")?.firstChild||null);b.onclick=()=>$("helpModal").style.display="flex"}if(!$("helpModal")){const m=document.createElement("div");m.id="helpModal";m.innerHTML=`<div class="help-card"><div style="display:flex;justify-content:space-between;align-items:center;gap:12px"><h2>사바나 달무티 게임 방법</h2><button class="btn ghost small" onclick="document.getElementById('helpModal').style.display='none'">닫기</button></div><div class="help-section"><strong>목표</strong><br>손패를 먼저 털수록 높은 순위를 얻고, 라운드마다 승점을 얻습니다.</div><div class="help-section"><strong>제출</strong><br>같은 계급 여러 장을 낼 수 있습니다. 이미 카드가 깔려 있으면 같은 장수이면서 더 높은 계급만 낼 수 있습니다.</div><div class="help-section"><strong>상납</strong><br>2라운드부터 하위 계급자가 상위 계급자에게 좋은 카드를 자동 상납하고, 받은 사람은 같은 장수만큼 돌려줍니다.</div><div class="help-section"><strong>민란</strong><br>농민 또는 노비가 홍길동 2장을 들면 계급 순서와 상납 방향이 뒤집힙니다.</div></div>`;document.body.appendChild(m);m.onclick=e=>{if(e.target===m)m.style.display="none"}}}
  function events(){E.homeBtn.onclick=()=>location.href="../";E.leaveRoomBtn.onclick=()=>leaveRoom();E.createRoomBtn.onclick=createRoom;E.refreshRoomsBtn.onclick=watchRooms;E.readyBtn.onclick=toggleReady;E.watchBtn.onclick=becomeSpectator;E.joinAsPlayerBtn.onclick=becomePlayer;E.startBtn.onclick=startGame;E.nextRoundBtn.onclick=()=>nextRound(false);E.resetGameBtn.onclick=resetGame;E.playBtn.onclick=playSelected;E.passBtn.onclick=passTurn;E.sendChatBtn.onclick=sendChat;E.chatInput.onkeydown=e=>{if(e.key==="Enter")sendChat()};E.toggleSpectatorChatBtn.onclick=toggleSpectatorChat;}
  async function init(){css();collect();ensureHelp();S.user=String(localStorage.getItem("partyAppUser")||"").trim();if(!S.user){alert("닉네임을 입력하세요.");return}E.myNickname.textContent=S.user;renderRanks();events();watchRooms();if(S.roomId){const ps=await partRef(S.roomId).doc(S.user).get().catch(()=>null);if(ps?.exists&&!ps.data().removedFromRoom)enterRoom(S.roomId);else localStorage.removeItem("dalmutiCurrentRoomId")}}
  window.Dalmuti={joinRoom,toggleRank,saveSettings,toggleSpectatorChat,kick,deleteRoom,forceRebellion:()=>nextRound(true)};
  window.addEventListener("DOMContentLoaded",init);
})();
